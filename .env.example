# Supabase PostgreSQL — pooled connection (for Prisma)
# Get from: Supabase Dashboard → Settings → Database → Connection string (URI) → add ?pgbouncer=true
DATABASE_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:6543/postgres?pgbouncer=true"

# Supabase PostgreSQL — direct connection (for migrations)
# Get from: Supabase Dashboard → Settings → Database → Connection string (URI) → use port 5432
DIRECT_URL="postgresql://postgres:[PASSWORD]@db.[PROJECT_REF].supabase.co:5432/postgres"

# Public URL of the deployed site
# For local dev: http://localhost:3000
# For production: https://emmanails.ro
NEXT_PUBLIC_SITE_URL="http://localhost:3000"

# Stripe (for future payments integration)
# Get from: https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY="sk_test_..."
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY="pk_test_..."
# STRIPE_WEBHOOK_SECRET="whsec_..."
